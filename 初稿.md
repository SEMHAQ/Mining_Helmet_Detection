# 基于ECB-YOLO的井下安全头盔检测模型
## 摘要
井下作业环境的安全监控是工业安全生产的核心环节，安全帽佩戴检测作为保障井下作业人员生命安全的关键任务，长期面临复杂背景干扰、低光照环境、小目标检测精度不足等诸多挑战。现有检测算法在多尺度特征融合、前景目标定位精度、动态上下文建模等方面存在明显局限，难以适配井下复杂场景的实时高精度检测需求。针对上述问题，本文以YOLOv11n为基础架构，提出一种面向井下场景的ECB-YOLO安全头盔检测模型，通过多维度协同优化策略实现检测性能的全面提升。模型引入EMSCP增强多尺度上下文感知模块，动态捕捉多尺度上下文信息，强化小目标特征提取能力；采用BiFPN双向加权特征融合网络，通过自适应空间权重实现多尺度特征的高效融合，缓解跨尺度特征冲突；设计ContextGuided上下文引导模块，通过多分支上下文信息建模优化前景区域特征，提升模型边界定位能力。基于公开井下安全帽数据集CUMT-HelmeT开展实验验证，结果表明，所提ECB-YOLO模型的mAP50达到86.63%，精确率达89.71%，召回率达78.59%，综合检测性能显著优于主流目标检测模型；同时模型参数量仅12.5M，计算量47.8 GFLOPs，推理速度达97.2 FPS，在提升检测精度的同时保持了优异的实时性，能够满足井下场景边缘设备部署的实际需求。

**关键词**：井下安全头盔检测；YOLOv11；BiFPN；上下文引导模块；多尺度特征融合

---

## 1 引言
### 1.1 研究背景与工程意义
矿业是我国能源与原材料供应的核心支柱产业，而井下作业环境具有高粉尘、低光照、空间受限、设备密集等天然特征，冒顶、机械碰撞、物体打击等安全事故风险始终居高不下。安全头盔作为井下作业人员最基础、最核心的头部防护装备，是抵御意外伤害的第一道防线，规范佩戴安全帽能够大幅降低井下事故的伤亡风险。然而受井下作业环境恶劣、工作强度大、人员安全意识不足等多重因素影响，不规范佩戴安全帽的违规行为在井下作业中屡禁不止。

传统的井下安全监管方式以人工巡检为主，存在巡检效率低、覆盖范围有限、存在监控盲区、主观性强等固有缺陷，且巡检人员自身也面临着与作业人员同等的井下安全风险。基于RFID、压力传感器的穿戴式监测方案，虽能实现一定程度的自动化监控，但存在硬件成本高、维护难度大、易受井下电磁环境干扰、需改造原有头盔结构等问题，难以在井下大规模推广应用。门禁式定点检测方案则只能在固定点位完成检测，无法实现作业全流程的连续监控，存在明显的监管滞后性。因此，构建智能化、自动化的井下安全帽实时检测系统，对实现井下作业人员防护状态的全时段监控与预警，从源头降低安全事故发生率，提升矿业安全生产整体水平具有至关重要的工程价值与现实意义。

### 1.2 研究现状与现存问题
近年来，深度学习技术在计算机视觉领域取得了突破性进展，基于卷积神经网络的目标检测算法为井下安全帽检测提供了全新的技术路径。相较于传统方法，基于深度学习的检测方法具备强大的特征提取能力、优异的环境适应性和高检测精度，能够自动学习复杂场景下的目标特征模式，有效应对井下环境的复杂性与多变性。当前主流的目标检测算法主要分为两类，一类是以R-CNN系列为代表的两阶段检测器，通过候选区域生成与精准分类两步完成检测，检测精度高但计算复杂度大，难以满足井下实时检测需求；另一类是以YOLO系列、SSD为代表的单阶段检测器，直接从特征图中预测目标位置与类别，在保证检测精度的同时具备更快的推理速度，更适配井下实时检测的应用场景。

在安全帽检测领域，国内外学者基于YOLO系列算法开展了大量优化研究，但现有方法在井下复杂场景中仍存在明显的技术瓶颈。井下低光照、高粉尘、强反光的极端环境会导致监控图像质量严重下降，目标特征模糊，现有算法的特征提取鲁棒性难以满足实际需求。远距离监控场景下，安全帽多以小目标形式呈现，且易被井下设备、人员频繁遮挡，现有算法极易出现漏检、误检问题。多尺度特征融合过程中，现有算法普遍存在跨尺度特征冲突问题，低层细节特征与高层语义特征无法实现有效协同，进一步限制了小目标检测性能的提升。同时，多数高精度优化方案会导致模型参数量与计算量大幅上升，难以在井下资源受限的边缘设备上实现实时部署，无法兼顾检测精度与推理效率。

### 1.3 本文研究内容与创新思路
针对上述研究空白与工程痛点，本文以YOLOv11n轻量级模型为基础架构，结合井下安全帽检测的任务特性与场景难点，提出面向井下复杂场景的ECB-YOLO安全帽检测模型。本文以井下复杂场景下的高精度、实时性检测为核心目标，从特征提取、特征融合、上下文建模三个维度对基础模型进行协同优化，构建完整的端到端检测链路。

本文的核心研究工作围绕井下场景的检测难点展开，通过多模块协同设计实现模型性能的全面提升。在骨干网络层面，设计多尺度特征提取结构，动态适配井下不同尺度的安全帽目标，增强低光照、强反光场景下的特征鲁棒性；在颈部网络层面，重构特征融合架构，解决跨尺度特征冲突问题，强化小目标细节特征的保留与传递；在特征优化层面，引入上下文引导机制，增强前景目标与复杂背景的特征区分度，提升遮挡场景下的目标边界定位精度。同时，所有改进模块均遵循轻量化设计原则，在提升检测精度的同时，保证模型能够适配井下边缘设备的实时部署需求。

## 2 相关工作
### 2.1 井下目标检测技术研究进展
井下场景的视觉目标检测是矿山智能化建设的核心研究方向之一，受井下复杂环境的限制，该任务对算法的鲁棒性、精度与实时性均提出了极高要求。近年来，众多学者基于深度学习算法开展了井下目标检测的优化研究，逐步形成了以YOLO系列算法为核心的技术体系。

针对井下低光照、高粉尘导致的图像质量下降问题，相关研究通过图像预处理与网络结构优化两条路径实现性能提升。有学者将AOD-Net去雾网络与改进YOLOv5算法结合，通过轻量化去雾预处理提升了复杂环境下的检测精度；也有研究通过设计PBSS-YOLOv8模型，引入SPD-Conv模块减少小目标信息损失，同时集成轻量化注意力机制缓解环境噪声干扰，有效提升了井下低光照环境下的安全帽检测性能。针对井下目标遮挡与小目标检测难题，现有研究主要从特征提取与特征融合两个维度进行优化，有学者提出MH-YOLO算法，通过卷积块注意力机制优化骨干网络，结合ZoomCat与Scalseq特征提取模块，提升了重叠与小目标的检测精度；也有研究基于YOLOv10框架提出YOLOv10-MSH模型，通过混合注意力机制融合空间与通道注意力，强化了特征提取能力，满足了井下煤矿场景安全帽实时检测的需求。

上述研究有效推动了井下目标检测技术的发展，但仍存在明显的技术瓶颈。多数改进方案仅针对井下单一环境挑战进行优化，难以同时应对低光照、遮挡、小目标、强反光等多重复杂因素的叠加影响；部分高精度优化方案以牺牲模型推理速度为代价，导致模型参数量与计算量大幅上升，无法适配井下边缘设备的实时部署需求。因此，如何在保持模型轻量性与实时性的前提下，全面提升模型在井下复杂场景的检测鲁棒性，仍是当前井下目标检测领域亟待解决的关键问题。

### 2.2 YOLO系列算法发展与优化现状
YOLO系列算法作为单阶段目标检测的标杆模型，凭借检测精度与推理速度的优异平衡，成为工业视觉检测领域的首选方案。从YOLOv1首次提出端到端的目标检测框架以来，YOLO系列持续迭代优化，不断突破性能边界。YOLOv5引入CSP结构与PAN-FPN特征融合网络，实现了精度与速度的进一步提升，成为工业界应用最广泛的版本；YOLOv7通过可扩展的聚合网络结构与动态标签分配策略，在相同算力下实现了更优的检测性能；YOLOv8对网络结构与损失函数进行全面重构，引入C2f模块与解耦头设计，进一步强化了多尺度目标检测能力；YOLOv11作为最新迭代版本，在C2f模块基础上优化为C3k2结构，集成C2PSA注意力模块，在大幅减少参数量与计算量的同时，提升了特征提取效率与多尺度检测性能，成为轻量级实时检测的优选基础架构。

针对YOLO系列算法的优化研究，当前主要集中在三个核心方向。其一是骨干网络的轻量化与特征提取能力增强，通过引入注意力机制、动态卷积等结构，提升模型对关键特征的捕捉能力，同时控制模型参数量；其二是特征融合网络的优化，通过BiFPN、ASFF等结构改进跨尺度特征融合方式，缓解特征冲突，强化小目标特征的保留与传递；其三是损失函数与检测头的优化，通过改进边界框回归损失与标签分配策略，提升目标定位精度与模型收敛速度。上述优化方向为本文的模型设计提供了重要的理论参考，本文结合井下安全帽检测的任务特性，从特征提取、特征融合、上下文建模三个维度对YOLOv11n进行协同优化，实现模型在井下复杂场景的性能全面提升。

## 3 ECB-YOLO模型整体架构与核心改进
### 3.1 基础YOLOv11网络架构
YOLOv11是Ultralytics团队推出的最新一代单阶段目标检测模型，其轻量级版本YOLOv11n在保持极低参数量与计算量的同时，具备优异的特征提取与检测性能，成为边缘设备部署的优选基础模型。YOLOv11n的整体架构由输入端、骨干网络、颈部网络与检测头四部分组成，形成了完整的端到端目标检测链路。

输入端采用自适应锚框动态生成算法与Mosaic数据增强策略，对输入图像进行预处理与数据增广，提升模型对不同尺度目标与复杂环境的泛化能力。骨干网络以CSPDarknet为基础架构，通过Conv卷积层、C3k2模块构建层级化特征提取结构，其中C3k2模块通过分支残差结构增强梯度传播能力，减少特征信息在传递过程中的损失；网络深层集成SPPF空间金字塔池化模块与C2PSA注意力模块，通过多尺度池化融合与通道-空间协同注意力，强化全局上下文信息捕捉能力，最终输出P3、P4、P5三个不同尺度的特征图。颈部网络采用PAN-FPN结构，通过自顶向下的FPN路径传递高层语义特征，结合自底向上的PAN路径传递低层细节特征，实现跨尺度特征的融合与增强。检测头采用解耦头结构，分别通过分类分支与回归分支完成目标类别预测与边界框回归，针对三个不同尺度的特征图输出最终检测结果。

尽管YOLOv11n在通用目标检测任务中表现优异，但在井下安全帽检测场景中仍存在明显的性能局限。固定感受野的卷积结构难以适配井下不同尺度的安全帽目标，尤其是远距离小目标的特征提取能力严重不足；PAN-FPN结构对不同尺度特征采用同等权重融合，无法自适应分配特征权重，易导致小目标细节特征丢失，存在严重的跨尺度特征冲突问题；同时模型对井下复杂背景的干扰抑制能力不足，前景目标的边界定位精度低，在遮挡、低光照场景下极易出现漏检、误检。针对上述问题，本文对YOLOv11n进行多维度协同优化，提出ECB-YOLO模型，全面提升其在井下复杂场景的检测性能。

### 3.2 ECB-YOLO整体网络架构
本文提出的ECB-YOLO模型完整保留了YOLOv11“骨干网络-颈部网络-检测头”的三段式核心架构，同时针对井下安全帽检测的任务特性，进行了三点核心架构优化，整体网络结构如图1所示。

在骨干网络优化层面，模型在浅层特征提取阶段引入C3k2_ContextGuided模块替换原始C3k2模块，强化前景目标的特征提取与边界定位能力；在深层特征提取阶段，将原始C3k2模块替换为C3k2_EMSCP模块，增强多尺度上下文信息捕捉能力，提升低光照、强反光场景下的特征鲁棒性。在颈部网络重构层面，模型采用BiFPN双向加权特征融合网络替换原始PAN-FPN结构，通过双向跨尺度连接与自适应权重学习，实现不同尺度特征图的高效融合，缓解跨尺度特征冲突，强化小目标特征保留。在检测头优化层面，模型在检测头前端集成ContextGuided上下文引导模块，对融合后的多尺度特征进行二次优化，进一步增强前景目标的特征响应，提升密集场景与遮挡场景下的目标定位精度。

通过上述架构改进，ECB-YOLO模型形成了“多尺度特征提取-自适应特征融合-上下文信息优化”的完整检测链路，三个改进模块相互协同，系统性解决了井下安全帽检测面临的小目标、遮挡、低光照、复杂背景干扰等核心难题，同时保持了模型的轻量性与实时性，能够充分适配井下边缘设备的部署需求。

### 3.3 核心改进模块设计
#### 3.3.1 EMSCP增强多尺度上下文感知模块
在井下安全帽检测任务中，不同距离的安全帽目标尺度差异极大，传统卷积操作的固定感受野难以动态适配多尺度目标的特征提取需求，易导致小目标特征丢失、大目标上下文信息捕捉不足的问题。针对这一问题，本文提出EMSCP增强多尺度上下文感知模块，以分组卷积为核心，构建多分支多尺度特征提取结构，实现对不同尺度安全帽目标的自适应特征捕捉。

模块的核心处理流程围绕多尺度特征提取与融合展开，输入特征图首先经过标准3×3卷积进行通道维度的初步特征变换，在不改变特征图分辨率的前提下，强化初始特征表达。完成初步变换后的特征图会被均匀分为M组，对每组特征图分别执行独立的线性变换操作，不同分支的线性变换采用不同膨胀率的卷积核，实现不同感受野的多尺度特征提取，分别捕捉小目标的细节特征与大目标的全局上下文信息。完成多分支特征提取后，对多尺度特征进行通道维度拼接，通过逐点卷积完成跨通道特征融合，消除分组卷积带来的通道间信息隔离问题，最终输出融合后的多尺度特征图。

相较于传统卷积结构，EMSCP模块通过分组多分支结构，在不显著增加参数量与计算量的前提下，实现了动态感受野的多尺度特征提取，有效适配井下不同尺度的安全帽目标。同时，多分支线性变换能够增强特征的鲁棒性，有效缓解低光照、强反光场景下的特征模糊问题，提升模型对复杂井下环境的适应能力。

#### 3.3.2 BiFPN双向加权特征融合网络
在单阶段目标检测任务中，多尺度特征融合的一致性是决定检测性能的关键因素。YOLOv11原始的PAN-FPN结构仅采用简单的跳层连接与特征拼接，对不同尺度的特征图采用同等权重进行融合，无法自适应区分不同特征的重要性，易导致高层语义特征与低层细节特征的冲突，尤其会造成井下小目标安全帽的细节特征丢失，限制了小目标检测性能的提升。针对这一问题，本文采用BiFPN双向加权特征融合网络替换原始PAN-FPN结构，对特征融合链路进行全面优化。

BiFPN针对传统特征金字塔网络的缺陷，进行了三点核心优化。在网络连接层面，模型在保留原始自顶向下与自底向上的两条特征融合路径的基础上，增加同一尺度特征的跳跃连接，将低层细节特征与高层语义特征直接融合，减少特征传递过程中的信息损失，同时简化网络结构，降低计算冗余。在特征融合机制层面，模型针对不同分辨率的输入特征图，引入可学习的权重参数，根据特征对检测任务的贡献度动态分配融合权重，强化对安全帽目标有效的特征信息，抑制背景无关特征，有效缓解跨尺度特征冲突问题。在特征处理层面，模型通过上采样与下采样操作，统一不同尺度特征图的分辨率与通道数，实现空间维度的精准对齐，确保特征融合的有效性，尤其强化小目标特征的保留与传递。

通过BiFPN结构的引入，模型能够有效融合不同尺度特征图中的语义信息与细节信息，自适应平衡不同特征的贡献度，显著缓解跨尺度特征冲突，大幅提升井下小目标安全帽、遮挡目标的检测性能。

#### 3.3.3 ContextGuided上下文引导模块
井下作业场景背景复杂，设备、管线、巷道结构等背景元素与安全帽目标的特征区分度低，且目标易被频繁遮挡，导致传统卷积结构难以精准区分前景与背景，目标边界定位精度不足，易出现漏检与误检。针对这一问题，本文设计ContextGuided上下文引导模块，通过多分支结构对局部特征、周围上下文特征与全局上下文特征进行协同建模，强化前景目标的特征响应，抑制背景干扰，提升模型的边界定位能力。

ContextGuided模块由四个核心分支构成，分别实现不同维度的上下文信息提取与融合。局部分支采用1×1卷积提取目标核心区域的局部细节特征，聚焦安全帽目标的核心纹理与边缘信息，为目标定位提供精准的细节支撑。周围分支采用大核卷积提取目标周边区域的上下文特征，捕捉目标与周围环境的空间关联信息，即使目标部分被遮挡，也能通过周边上下文信息实现目标的精准定位。联合分支将局部分支与周围分支提取的特征进行通道维度拼接，通过BN层与PReLU激活函数进行非线性变换，实现局部细节特征与周围上下文特征的深度融合，增强特征的判别能力。全局分支通过全局平均池化压缩特征图的空间维度，结合两层全连接层捕捉全局上下文信息，学习不同通道特征的重要性权重，最终通过通道注意力机制对融合特征进行加权，强化对安全帽目标有效的通道特征，抑制背景无关通道的干扰。模块的最终输出为全局分支加权后的特征图，实现对前景目标特征的全面优化。

ContextGuided模块通过多分支协同建模，同时捕捉目标的局部细节、周边上下文与全局语义信息，能够有效区分井下复杂背景中的前景安全帽目标，显著提升遮挡、低对比度场景下的目标边界定位精度，减少漏检与误检。

## 4 实验设计与结果分析
### 4.1 实验数据集与预处理
为验证所提ECB-YOLO模型的有效性，本文采用公开井下安全帽数据集CUMT-HelmeT开展实验验证，该数据集采集自真实煤矿井下作业场景，涵盖了低光照、高粉尘、遮挡、强反光、小目标等多种典型井下复杂工况，与实际工程应用场景高度契合，能够充分验证模型在井下场景的实际性能。

数据集原始包含2482张井下作业监控图像，共标注6个类别，分别为未佩戴安全帽、佩戴安全帽、井下车辆、普通帽子、单人、人群，其中核心检测类别佩戴安全帽包含1006张标注样本，未佩戴安全帽包含1592张标注样本。针对原始数据集样本量有限、类别分布不均衡的问题，本文采用数据增强策略对数据集进行扩充，通过随机裁剪、平移、旋转、镜像翻转、亮度调整、对比度调整、噪声叠加等多种方式，模拟井下不同光照、角度、环境下的目标形态变化，将数据集扩充至原始的5倍，最终得到12410张高质量标注图像。完成数据扩充后，将数据集按照7:2:1的比例随机划分为训练集、验证集与测试集，其中训练集用于模型参数学习，验证集用于训练过程中的超参数动态调整与过拟合防控，测试集用于模型最终性能的客观评估，确保实验结果的真实性与可重复性。

### 4.2 实验环境与参数设置
为保证实验的可靠性与可复现性，本文搭建了统一的实验软硬件环境，所有模型均在相同环境下完成训练与测试，硬件环境采用搭载18核AMD EPYC 9754处理器、NVIDIA RTX 4090D 24GB显卡的服务器，操作系统为Ubuntu 22.04，软件环境基于Python 3.12.0搭建，深度学习框架采用PyTorch 2.5.1，GPU加速库为CUDA 12.4。

模型训练过程中，统一设置训练轮次为100，批次大小为32，输入图像尺寸为640×640，优化器采用SGD，初始学习率设置为0.01，动量为0.973，权重衰减为0.0005，框损失权重为7.5，分类损失权重为0.5，分布焦点损失权重为1.5。同时，训练过程中采用预热学习率策略，前3轮训练进行学习率预热，避免模型初期梯度爆炸；结合Mosaic数据增强、标签平滑等正则化策略，提升模型的泛化能力，防止过拟合。

### 4.3 模型评价指标
本文采用目标检测领域通用的量化评价指标，从检测精度与推理性能两个维度对模型进行全面评估。精度评价指标包括精确率、召回率、F1分数、IoU阈值为0.5时的平均精度均值，其中精确率表示模型检测为正的样本中真实正样本的占比，用于衡量模型的检测准确程度，减少误检；召回率表示所有真实正样本中被模型正确检测的占比，用于衡量模型的检测全面程度，减少漏检；F1分数为精确率与召回率的调和平均数，用于综合衡量模型的整体检测性能；平均精度均值是目标检测任务中最核心的综合精度评价指标，能够全面反映模型对所有类别的检测能力。

推理性能评价指标包括参数量、计算量、每秒推理帧数、单张图像推理时间，用于衡量模型的轻量性与实时性，验证模型在井下边缘设备的部署可行性。所有评价指标均采用目标检测领域通用的计算公式进行量化计算，确保评价结果的客观性与可比性。

### 4.4 消融实验与结果分析
为量化验证本文所提三个改进模块的有效性与协同作用，以YOLOv11n为基线模型，在CUMT-HelmeT测试集上开展控制变量消融实验，通过逐一添加、组合改进模块，量化各模块对模型性能的贡献度，实验结果如表2所示。

从消融实验结果可以看出，三个改进模块单独引入时，均能提升基线模型的综合检测性能，验证了各模块设计的有效性。其中单独引入BiFPN模块后，模型的平均精度均值从0.8315提升至0.8481，F1分数提升至0.8196，是单模块优化中性能提升最显著的，说明多尺度特征融合冲突是限制基线模型性能的核心瓶颈，BiFPN的自适应加权融合有效缓解了这一问题。单独引入ContextGuided模块后，模型的召回率与精确率均有明显提升，说明该模块有效增强了前景目标定位能力，减少了井下场景的漏检问题。单独引入EMSCP模块后，模型的精确率提升至0.8767，说明多尺度特征提取能力得到了显著增强，有效适配了井下不同尺度的安全帽目标。

两个模块组合引入时，模型性能呈现进一步提升，其中EMSCP与BiFPN的组合效果最优，平均精度均值达到0.8612，相较于基线模型提升了2.97个百分点，说明两个模块具备良好的协同效应，EMSCP模块提取的多尺度高质量特征，通过BiFPN模块实现了高效的跨尺度融合，二者形成了“特征提取-特征融合”的协同优化链路，实现了性能的叠加提升。三个模块全部引入后，最终的ECB-YOLO模型取得了最优的检测性能，平均精度均值达到0.8663，相较于基线YOLOv11n提升了3.48个百分点，精确率提升至0.8971，F1分数提升至0.8376。实验结果表明，三个模块形成了完整的协同优化体系，EMSCP模块提供多尺度高质量特征，BiFPN模块实现跨尺度特征的自适应融合，ContextGuided模块进一步优化前景特征与边界定位，三者相互配合，系统性解决了井下安全帽检测的核心难点，实现了检测性能的全面提升。

### 4.5 主流模型对比实验
为全面验证所提ECB-YOLO模型的综合性能，本文选取当前主流的目标检测模型进行对比实验，包括传统单阶段检测器SSD、两阶段检测器Faster R-CNN与RetinaNet、Transformer类检测器DETR，以及YOLO系列主流模型YOLOv9、YOLOv10、基线模型YOLOv11n，所有模型均在相同的实验环境、数据集与训练参数下完成训练与测试，实验结果分为检测精度与推理性能两部分。

检测精度对比结果显示，本文提出的ECB-YOLO模型在所有核心精度指标上均显著优于其他主流模型。相较于传统单阶段检测器SSD，ECB-YOLO的平均精度均值提升了42.43个百分点；相较于两阶段经典模型Faster R-CNN，平均精度均值提升了27.73个百分点；相较于YOLO系列主流模型YOLOv9，平均精度均值提升了9.23个百分点；相较于基线模型YOLOv11n，平均精度均值提升了3.48个百分点，精确率提升了5.7个百分点。实验结果充分表明，本文的改进策略有效解决了井下复杂场景安全帽检测的核心难点，模型在低光照、遮挡、小目标等复杂工况下的检测精度与鲁棒性得到了显著提升，能够有效减少井下安全帽检测的误检与漏检，满足井下安全监管的高精度需求。

推理性能对比结果显示，ECB-YOLO模型在提升检测精度的同时，保持了优异的轻量性与实时性。模型参数量仅12.5M，相较于基线YOLOv11n减少了5.3%，计算量47.8 GFLOPs，减少了3.4%；推理速度达到97.2 FPS，单张图像推理时间仅10.3ms，相较于YOLOv10提升了11.3%，相较于YOLOv9提升了42.1%，更是远超两阶段模型Faster R-CNN。实验结果表明，本文的改进策略在提升模型检测精度的同时，并未增加模型的计算负担，反而通过轻量化的模块设计进一步降低了参数量与计算量，实现了检测精度与推理速度的优异平衡，能够满足井下边缘设备实时检测的部署需求。

### 4.6 典型场景可视化结果分析
为直观验证ECB-YOLO模型在井下复杂场景的检测性能，本文选取井下最具代表性的四种典型挑战场景：严重遮挡、极低光照、小目标、强反光，对不同模型的检测结果进行可视化对比分析。

在严重遮挡场景中，当安全帽40%-50%的区域被井下设备遮挡时，SSD、RetinaNet等传统模型的检测置信度显著下降，YOLOv11n的检测置信度为0.79，而ECB-YOLO模型的置信度达到0.80，能够精准识别部分遮挡的安全帽目标，这得益于ContextGuided模块的多分支上下文建模，通过目标周边的上下文信息实现了遮挡目标的精准定位，有效解决了井下遮挡场景的漏检问题。在极低光照场景中，井下巷道低光照环境导致图像整体亮度低、目标与背景对比度差，SSD模型的检测置信度仅0.42，YOLOv11n置信度为0.71，而ECB-YOLO模型的置信度达到0.80，在低光照环境下仍保持了稳定的检测性能，这是因为EMSCP模块的多尺度特征提取增强了低对比度区域的特征响应，有效捕捉了低光照下安全帽的微弱特征，提升了模型对暗环境的适应能力。

在小目标场景中，远距离监控下的安全帽以小目标形式呈现，特征信息极少，SSD模型出现完全漏检，YOLOv11n的检测置信度仅0.56，而ECB-YOLO模型的置信度达到0.66，能够精准识别远距离小目标安全帽，这得益于BiFPN模块的双向加权特征融合，有效保留了小目标的细节特征，同时结合高层语义信息实现了小目标的精准识别，大幅减少了远距离监控的漏检问题。在强反光场景中，井下矿灯的强光照射导致安全帽表面出现镜面反光，局部特征被严重破坏，SSD模型出现完全漏检，YOLOv11n的检测置信度仅0.52，而ECB-YOLO模型的置信度达到0.72，仍能稳定检测目标，这是因为EMSCP模块的全局上下文信息捕捉，有效缓解了局部强光的特征干扰，结合ContextGuided模块的全局注意力机制，实现了强反光场景下的鲁棒检测。

可视化结果充分验证了ECB-YOLO模型在井下多种复杂场景下的优异检测性能，相较于主流模型，所提模型在遮挡、低光照、小目标、强反光等极端工况下，仍能保持高置信度的稳定检测，具备极强的环境适应性与工程实用价值。

## 5 结论
针对井下复杂场景安全头盔检测面临的低光照、遮挡、小目标、强反光等核心挑战，本文以YOLOv11n轻量级模型为基础，提出了一种面向井下场景的ECB-YOLO安全头盔检测模型。通过EMSCP增强多尺度上下文感知模块，实现了不同尺度安全帽目标的自适应特征提取，提升了低光照、强反光场景下的特征鲁棒性；采用BiFPN双向加权特征融合网络，实现了跨尺度特征的自适应高效融合，缓解了特征冲突，强化了小目标特征保留；设计ContextGuided上下文引导模块，通过多分支上下文建模增强了前景目标定位能力，有效应对遮挡场景的检测难题。

在CUMT-HelmeT井下安全帽公开数据集上的系统实验表明，所提ECB-YOLO模型的平均精度均值达到86.63%，相较于基线YOLOv11n提升了3.48个百分点，精确率达89.71%，综合检测性能显著优于SSD、Faster R-CNN、YOLOv9、YOLOv10等主流目标检测模型；同时模型参数量仅12.5M，推理速度达97.2 FPS，在提升检测精度的同时保持了优异的轻量性与实时性，能够有效应对井下多种复杂工况的检测需求，适配边缘设备的实时部署。

未来的研究工作将从两个方向展开，一是构建多光照、多视角、多工况的大规模井下安全帽数据集，进一步提升模型的泛化能力与环境适应性；二是探索轻量化注意力机制与动态卷积的融合范式，在保持检测精度的同时进一步压缩模型体积，优化模型在嵌入式边缘设备的部署效果，同时结合多目标跟踪算法，实现井下作业人员安全帽佩戴状态的全流程连续监控，为矿山智能化安全生产提供更全面的技术支撑。

